# =============================================================================
# ENVIRONMENT VARIABLES - COPY THESE TO RENDER DASHBOARD
# =============================================================================
# DO NOT commit this file with actual values. This is just a template.
# Replace all placeholder values with your actual credentials in Render Dashboard

# Production Database (Render PostgreSQL)
DATABASE_URL=postgresql://user:password@host:5432/database_name

# Node Environment
NODE_ENV=production

# Supabase Configuration (Required for authentication & storage)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Google OAuth (Optional - required only if using Google login)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Facebook OAuth (Optional - required only if using Facebook login)
FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_APP_SECRET=your-facebook-app-secret
